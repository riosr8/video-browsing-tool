const chai = require('chai');

// eslint-disable-next-line prefer-destructuring
const expect = chai.expect;

describe('Backend Canary Test', () => {
  it('canary', () => {
    expect(true).to.equal(true);
  });
});
